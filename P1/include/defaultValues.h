#ifndef DEFAULT_VALUES_H
#define DEFAULT_VALUES_H

// значения по умолчанию

static const int DEFAULT_INTERFACE = 0; // номер сетевого интерфейса
static const int DEFAULT_SNIFF_TIME = 10; // время захвата пакетов (в сек.)
static const char* DEFAULT_PCAP_FILE = (char*)"packets.pcap"; // pcap-файл
static const char* DEFAULT_CSV_FILE = (char*)"netflow.csv"; // csv-файл

#endif